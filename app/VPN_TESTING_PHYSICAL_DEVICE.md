# Тестирование VPN на физическом устройстве

## ⚠️ ВАЖНО: Эмулятор НЕ поддерживает VPN

### Почему VPN не работает в эмуляторе?
- **Google Android Emulator** не поддерживает полную функциональность VPN на уровне нативного кода
- wireguard_flutter требует доступа к системным средствам VPN, которые недоступны в эмуляторе
- TUN интерфейс не создаётся в эмуляторе

### Что нужно для тестирования?
✅ **Физическое Android устройство** (телефон/планшет)
✅ **Android версия 6.0+ (API 24+)**
✅ **USB кабель для подключения к ПК**

---

## 📱 Инструкция по установке и тестированию

### Шаг 1: Подготовка устройства

1. **Включить режим разработчика**
   - Настройки → О телефоне
   - Нажать 7 раз на "Номер сборки"
   - Вернуться в Настройки → Параметры разработчика
   - Включить "Отладка по USB"

2. **Разрешить установку из неизвестных источников**
   - Настройки → Приложения
   - Меню → Специальный доступ
   - Установка приложений из неизвестных источников → Разрешить для файлового менеджера

3. **Подключить устройство к ПК по USB**

### Шаг 2: Установить APK

```bash
# Открыть PowerShell в папке проекта
cd c:\vpn

# Проверить, что устройство видно
flutter devices

# Установить приложение на устройство
flutter install

# ИЛИ использовать встроенный APK (если выше не работает):
# Найти путь к Android SDK
Get-Command adb

# Или явно:
$env:ANDROID_HOME = "$env:USERPROFILE\AppData\Local\Android\Sdk"
& "$env:ANDROID_HOME\platform-tools\adb.exe" install -r build\app\outputs\flutter-apk\app-dev-debug.apk
```

### Шаг 3: Первый запуск

1. **Открыть приложение** на устройстве
2. **Зарегистрироваться/Логиниться** с тестовым аккаунтом
3. **Перейти на Home screen**

### Шаг 4: Тестирование VPN кнопки

**При нажатии на VPN кнопку:**

#### Что ДОЛЖНО произойти:
- Система запросит разрешение на использование VPN
- Откроется диалог Android: *"Приложение хочет создать VPN соединение"*
- После подтверждения кнопка визуально активируется
- В верхней панели появится иконка VPN 🔒

#### Что ПРОВЕРИТЬ:
- ✅ Диалог разрешения Android появился?
- ✅ После нажатия OK кнопка в приложении активирована?
- ✅ Иконка VPN в статус-баре Android?
- ✅ Трафик проходит через WireGuard?

### Шаг 5: Проверка логов

```bash
# Смотреть логи приложения в реальном времени
flutter logs

# Или через ADB:
$env:ANDROID_HOME = "$env:USERPROFILE\AppData\Local\Android\Sdk"
& "$env:ANDROID_HOME\platform-tools\adb.exe" logcat | Select-String "VpnManager|wireguard"
```

**Ищи в логах:**
```
[INFO] VpnManager: Инициализирую WireGuard интерфейс wg0
[INFO] VpnManager: Запускаю VPN с конфигом
[INFO] VpnManager: VPN туннель успешно активирован
```

---

## 🔍 Возможные проблемы

### 1. "VPN соединение уже активно"
- Отключи существующие VPN в Настройки → Сеть
- Очисти данные приложения: Настройки → Приложения → Incamp VPN → Хранилище → Очистить

### 2. "Permission denied" в логах
- Проверь разрешения: Настройки → Приложения → Incamp VPN → Разрешения
- Убедись что INTERNET разрешен
- На Android 6-11: разрешения должны запрашиваться при первом использовании

### 3. "WireGuard интерфейс не инициализирован"
- Проверь, не установлено ли уже другое VPN приложение
- Перезагрузи устройство

### 4. Приложение крашится при нажатии VPN
- Проверь логи: `flutter logs`
- Ищи исключения в `VpnManager.connect()`
- Убедись что `fetchWgQuick()` возвращает корректный конфиг

---

## 📋 Чек-лист перед тестированием

- [ ] Физическое Android устройство подключено
- [ ] `flutter devices` показывает устройство
- [ ] APK успешно установлен
- [ ] User зарегистрирован и залогинен
- [ ] Subscription активна (или используется для тестирования)
- [ ] В AndroidManifest.xml есть VPN разрешения
- [ ] wireguard_flutter плагин в pubspec.yaml

---

## 🚀 Команды для быстрого тестирования

```bash
# Перейти в проект
cd c:\vpn

# Clean & rebuild
flutter clean
flutter pub get

# Установить на устройство и запустить
flutter run -t lib/main.dart --flavor dev

# С логами в реальном времени
flutter run -t lib/main.dart --flavor dev & flutter logs

# Установить APK без запуска
adb install -r build/app/outputs/flutter-apk/app-dev-debug.apk
```

---

## ℹ️ Информация о WireGuard сервере

**Адрес:** `62.84.98.109:51820`
**Статус:** Проверен и работает
**Конфиг:** Генерируется бэком при создании peer
**Шифрование:** WireGuard нативное (ядро Linux)

---

## 📞 Контакты для отладки

Если VPN всё ещё не работает на реальном устройстве:

1. **Проверь**: `flutter logs` на предмет ошибок в `VpnManager`
2. **Проверь**: Логи Android: `adb logcat | grep wireguard_flutter`
3. **Проверь**: Все ли разрешения даны приложению
4. **Проверь**: Нет ли конфликтующих VPN в системе

---

## ✅ Успешное тестирование

Если после нажатия VPN кнопки:
- ✅ Видна иконка VPN в статус-баре
- ✅ Логи показывают "VPN успешно запущен"
- ✅ Интернет работает (трафик идёт через VPN)
- ✅ Отключение кнопки корректно отключает VPN

То VPN интеграция работает корректно! 🎉
