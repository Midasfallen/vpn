# 🔑 Добавление публичного ключа на production сервер

## Публичный ключ для добавления:

```
ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAILeLw/asgE7dzrioeW8UQispbKEb9LygFMDrjEDRX8Af vpn-deploy
```

## Инструкция для добавления на сервер 146.103.99.70:

### Способ 1: SSH (если у вас уже есть доступ)

```bash
# На машине с доступом к серверу:
ssh root@146.103.99.70 << 'EOF'
mkdir -p ~/.ssh
cat >> ~/.ssh/authorized_keys << 'KEYEOF'
ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAILeLw/asgE7dzrioeW8UQispbKEb9LygFMDrjEDRX8Af vpn-deploy
KEYEOF
chmod 700 ~/.ssh
chmod 600 ~/.ssh/authorized_keys
echo "✅ Публичный ключ добавлен!"
EOF
```

### Способ 2: Вручную на сервере

SSH на 146.103.99.70 и выполни:

```bash
mkdir -p ~/.ssh
cat >> ~/.ssh/authorized_keys << 'EOF'
ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAILeLw/asgE7dzrioeW8UQispbKEb9LygFMDrjEDRX8Af vpn-deploy
EOF
chmod 600 ~/.ssh/authorized_keys
```

## Проверка

После добавления ключа, попробуй подключиться:

```bash
ssh -i ~/.ssh/id_ed25519 root@146.103.99.70 "echo ✅ SSH работает!"
```

Если появилось "✅ SSH работает!" — всё готово к Deploy!

## Почему это нужно

GitHub Actions использует этот SSH ключ для:
1. Подключения к серверу по SSH
2. Передачи .env файла на сервер (SCP)
3. Запуска команд для перезапуска Docker сервисов

Без добавленного публичного ключа GitHub Actions не может аутентифицироваться на сервере.
